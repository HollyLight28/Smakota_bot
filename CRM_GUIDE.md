# 🚀 Smakota CRM: Інструкція для бізнесу

Цей бот тепер працює як повноцінна система управління доставкою (CRM).

## 👨‍💼 Для Власника / Диспетчера (Адміністратора)

### 1. Налаштування кур'єрів
Спочатку додайте всіх своїх кур'єрів у систему. Вам потрібно знати їх Telegram ID (вони можуть дізнатися його у бота @userinfobot).

**Команда:** `/add_courier <ID> <Ім'я>`

**Приклад:**
```
/add_courier 7581726569 Вова
/add_courier 123456789 Петя
```

### 2. Прийом замовлень
Коли клієнт робить замовлення:
1. Ви отримуєте повідомлення в чат з деталями.
2. Під повідомленням з'явиться кнопка **"✅ Прийняти"**.
3. Натисніть її, і бот покаже список доступних кур'єрів (Вова, Петя...).
4. Натисніть на кур'єра, щоб призначити йому замовлення.

Статус замовлення зміниться на "В доставці", а кур'єр отримає сповіщення.

### 3. Контроль каси
В кінці зміни кожен кур'єр пише команду `/my_report` і скидає скріншот. Там буде вказано, скільки готівки він має здати.

---

## 🛵 Для Кур'єра

### 1. Отримання замовлення
Коли диспетчер призначає вам замовлення, вам приходить повідомлення:
*   Деталі замовлення (що везти).
*   Адреса і телефон клієнта.
*   Сума до оплати (і тип оплати: Готівка/Термінал).
*   **Кнопка "🗺️ Відкрити карту"** — відкриває Google Maps з прокладеним маршрутом.

### 2. Завершення доставки
Коли ви віддали замовлення і отримали гроші:
1. Натисніть кнопку **"✅ ДОСТАВИВ"** під повідомленням замовлення.
2. Це зафіксує доставку і додасть суму в ваш денний звіт.

### 3. Здача зміни
В кінці дня напишіть боту:
`/my_report`

Бот покаже:
*   Скільки замовлень ви відвезли.
*   Скільки готівки ви повинні здати в касу.

---

## ⚙️ Технічна інформація

### Оновлення Меню
Меню тепер завантажується з файлу `update_menu_gs.py`. Щоб оновити ціни або страви:
1. Відредагуйте `MENU_DATA` в файлі `update_menu_gs.py`.
2. В чаті з ботом (як адмін) напишіть `/updatemenu`.

### База Даних
Всі замовлення зберігаються в файлі `smakota.db`. Це надійно і дозволяє будувати звіти за будь-який період (потрібно буде додати команду для вигрузки історії).
