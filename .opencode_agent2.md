Потрібно додати роль Кухня до бота Smakota Bot. Проект в /home/aiuser/code/Smakota_bot/

## Що зробити:

### 1. В callbacks.py (bot/handlers/callbacks.py)
Після блоку `elif role == "hall":` (рядок ~48) додати:
```python
elif role == "kitchen":
    if not any(c['chat_id'] == user_id for c in db.get_chefs()):
        db.add_chef(f"Кухар_{call.from_user.first_name}", user_id)
    bot.edit_message_text("👩‍🍳 Інтерфейс змінено на **Кухня**.", call.message.chat.id, call.message.message_id, reply_markup=None, parse_mode='Markdown')
    bot.send_message(user_id, "Панель кухні:", reply_markup=keyboards.get_kitchen_keyboard())
```

### 2. В role_selection keyboard (keyboards.py, функція get_role_selection_keyboard)
Додати кнопку `👩‍🍳 Кухня` з callback `set_role_kitchen` до клавіатури вибору ролей.

### 3. Створити файл bot/handlers/kitchen.py
Базовий хендлер для кнопок кухні:
```python
import telebot
from telebot.types import Message
from bot import bot
from bot.utils import logger
import database as db

@bot.message_handler(func=lambda msg: msg.text == '🛒 Список закупів')
def handle_shopping_list(msg):
    """Показує список закупів."""
    from keyboards import get_kitchen_keyboard
    depts = db.get_departments()
    markup = telebot.types.InlineKeyboardMarkup(row_width=1)
    for d in depts:
        markup.add(telebot.types.InlineKeyboardButton(d['name'], callback_data=f"shop_dept_{d['id']}"))
    bot.send_message(msg.chat.id, "📋 **Список закупів**\\nОбери цех:", reply_markup=markup, parse_mode='Markdown')

@bot.message_handler(func=lambda msg: msg.text == '📝 Записник для Шефа')
def handle_notepad(msg):
    """Місце для майбутнього записника."""
    bot.send_message(msg.chat.id, "📝 Записник — функція в розробці. Буде доступна найближчим часом.")
```

### 4. Підключити kitchen.py в bot/__init__.py
Додати імпорт `from bot.handlers import kitchen` (або в те місце, де імпортуються інші хендлери).

### 5. Не змінювати нічого іншого
Тільки ці 4 зміни.

Після змін:
- Запусти `python3 -c "import py_compile; py_compile.compile('bot/handlers/callbacks.py', doraise=True); py_compile.compile('bot/handlers/kitchen.py', doraise=True); py_compile.compile('keyboards.py', doraise=True); print('OK')"` — перевірити синтаксис
- Зроби commit: git add -A && git commit -m "feat: додано роль Кухня + базовий хендлер shopping list"
