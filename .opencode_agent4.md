Задача: Створити інтерфейс для Шефа (admin role) — перегляд списку закупів + позначка куплено/не куплено.

Проект: /home/aiuser/code/Smakota_bot

## Контекст
- Вже є active_shopping_list таблиця + функції (get_active_shopping_list(), mark_as_purchased(), clear_todays_list())
- Адмін (Шеф) має свою клавіатуру в keyboards.py: get_admin_keyboard()
- Шеф — це ADMIN_ID з config.py
- Роль admin вже працює через callback set_role_admin

## Що зробити

### 1. Додати кнопку "📋 Список закупів" в адмін-клавіатуру (keyboards.py)
В функцію `get_admin_keyboard()` додати кнопку `'📋 Список закупів'`.
Поточна клавіатура:
```python
keyboard.row('📋 Нові замовлення', '📊 Виручка за сьогодні')
keyboard.row('📣 Розсилка', '📊 Моніторинг')
```
Зробити:
```python
keyboard.row('📋 Нові замовлення', '📋 Список закупів')
keyboard.row('📣 Розсилка')
```

### 2. Створити bot/handlers/admin.py (або додати хендлер в існуючий файл)
Якщо файлу admin.py немає — створити. Якщо є — додати туди.

Хендлер для "📋 Список закупів":
```python
@bot.message_handler(func=lambda msg: msg.text == '📋 Список закупів')
def admin_show_shopping_list(msg):
    """Шеф бачить список закупів."""
    items = db.get_active_shopping_list()
    if not items:
        bot.send_message(msg.chat.id, "✅ **Список закупів порожній.** Все є в наявності!", parse_mode='Markdown')
        return
    
    markup = InlineKeyboardMarkup(row_width=1)
    msg_text = "📋 **Список закупів**\n\n"
    for item in items:
        status_icon = "✅" if item['is_purchased'] else "⬜"
        msg_text += f"{status_icon} **{item['name']}** — {item['quantity']}\n"
        if not item['is_purchased']:
            markup.add(InlineKeyboardButton(
                f"✅ Куплено: {item['name']}",
                callback_data=f"shop_buy_{item['id']}"
            ))
    
    markup.add(InlineKeyboardButton("🔄 Очистити список", callback_data="shop_clear"))
    bot.send_message(msg.chat.id, msg_text, reply_markup=markup, parse_mode='Markdown')
```

### 3. Додати callback for "shop_buy_" і "shop_clear" в callbacks.py
```python
# === Shopping List Admin Callbacks ===
if data.startswith("shop_buy_"):
    list_id = int(data.replace("shop_buy_", ""))
    db.mark_as_purchased(list_id)
    bot.answer_callback_query(call.id, "✅ Позначено як куплено!")
    # Оновити повідомлення
    items = db.get_active_shopping_list()
    markup = InlineKeyboardMarkup(row_width=1)
    msg_text = "📋 **Список закупів**\n\n"
    for item in items:
        status_icon = "✅" if item['is_purchased'] else "⬜"
        msg_text += f"{status_icon} **{item['name']}** — {item['quantity']}\n"
        if not item['is_purchased']:
            markup.add(InlineKeyboardButton(
                f"✅ Куплено: {item['name']}",
                callback_data=f"shop_buy_{item['id']}"
            ))
    if items:
        markup.add(InlineKeyboardButton("🔄 Очистити список", callback_data="shop_clear"))
    bot.edit_message_text(msg_text, call.message.chat.id, call.message.message_id, reply_markup=markup, parse_mode='Markdown')

if data == "shop_clear":
    db.clear_todays_list()
    bot.edit_message_text("✅ Список закупів очищено.", call.message.chat.id, call.message.message_id)
    bot.answer_callback_query(call.id, "🧹 Список очищено!")
```

ВАЖЛИВО: Ці callback-и мають бути доступні ТІЛЬКИ для admin (user_id == ADMIN_ID). Додати перевірку:
```python
if user_id != ADMIN_ID:
    bot.answer_callback_query(call.id, "❌ Доступно тільки Шефу")
    return
```

### 4. Підключити admin.py хендлер
Перевірити що він імпортується в bot/handlers/__init__.py

### 5. Після змін
- Перевірити синтаксис
- Зробити commit: git add -A && git commit -m "feat: шеф - перегляд списку закупів + куплено/не куплено"
